# Orca v0.3.11 Reliability Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish Orca v0.3.11 as a reliability follow-up covering headless trajectory truth, runtime-owned cancellation, MCP SSE elicitation, inherited network grants, JSONL approvals, and governed automatic memory.

**Architecture:** The release contains the already integrated runtime and contract slices on local `main`. Release preparation changes version metadata, public notes, and site listings only; the tag workflow remains the sole binary and npm publisher.

## Task 1: Freeze the candidate and baseline

- [x] Confirm local `main` contains the reviewed reliability commits and no unrelated working-tree changes.
- [x] Confirm `v0.3.10` is already public and do not move or reuse its tag.
- [x] Confirm `v0.3.11` is absent locally, remotely, and from npm before versioning.

## Task 2: Prepare synchronized metadata

- [x] Set `Cargo.toml`, `Cargo.lock`, and `npm/orca/package.json` to `0.3.11`.
- [x] Add `docs/releases/v0.3.11.md` with compatibility, verification, and upgrade instructions.
- [x] Add English and Chinese website summaries, the top release entry, and current sitemap dates.
- [x] Run version synchronization and metadata self-tests after the version changes.

## Task 3: Execute the pre-release gate

- [x] Run the full locked workspace nextest gate: 2,573/2,573 passed.
- [x] Run the full locked MCP package gate: 48/48 passed.
- [x] Run formatter, clippy, runtime-surface, Windows-boundary, TUI serial, release self-tests, site build, SEO, and diff checks after versioning.
- [x] Re-run the real DeepSeek and TUI/ACP/server harnesses against the release candidate binary.

## Task 4: Qualify and publish

- [ ] Commit synchronized release metadata and notes.
- [ ] Verify local `main`, `origin/main`, and the release commit match before push.
- [ ] Push `main`, wait for required native CI and Pages checks, then create and push annotated tag `v0.3.11`.
- [ ] Monitor the sole release workflow through native artifacts, GitHub Release, npm publication, and public verifier.
- [ ] Verify installation, package versions, release visibility, and Pages changelog before cleaning only merged worktrees.
